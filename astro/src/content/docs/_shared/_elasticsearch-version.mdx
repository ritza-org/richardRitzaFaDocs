From version `1.42.0`, FusionAuth currently supports:

- Elasticsearch versions `7.6.1` through `9.4.1`.
- OpenSearch versions `2` through `3.6.0`.

Later search versions may work, but have not been tested.